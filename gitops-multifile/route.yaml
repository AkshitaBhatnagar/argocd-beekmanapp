apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: bee
  name: bee
  namespace: argocd-test
spec:
  port:
    targetPort: http
  to:
    kind: Service
    name: bee
    weight: 100
  wildcardPolicy: None
